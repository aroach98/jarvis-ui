# jarvis-core

Headless Node/TS service. Owns the Jarvis master agent, the 5 domain subagents, the
voice pipeline (wake word, STT, TTS), and every data connector. See the repo root
`ARCHITECTURE.md` §3 and §4. Not yet implemented — Phase 2/3 of `ROADMAP.md`.
