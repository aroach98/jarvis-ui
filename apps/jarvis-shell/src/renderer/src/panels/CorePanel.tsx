/**
 * TODO(Phase 1): port from the approved mockup — arc-reactor visualizer,
 * listening/routing status, free/pro mode toggle (manual click for now;
 * voice-driven toggling is Phase 3). Render from `@jarvis-ui/shared`'s
 * `CorePanelState`.
 */
export function CorePanel(): JSX.Element {
  return <div>Core panel (Jarvis) — not yet built</div>;
}
