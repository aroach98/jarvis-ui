/**
 * TODO(Phase 1): port from the approved mockup — CACC inbox, fleet,
 * checks & deploys. Render from `@jarvis-ui/shared`'s `CaccPanelState`.
 */
export function LeftPanel(): JSX.Element {
  return <div>Left panel (CACC) — not yet built</div>;
}
