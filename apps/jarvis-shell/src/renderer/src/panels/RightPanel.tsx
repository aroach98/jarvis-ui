/**
 * TODO(Phase 1): port from the approved mockup — Momentum inbox, fleet,
 * CRM pipeline, outreach placeholder. Render from `@jarvis-ui/shared`'s
 * `MomentumPanelState`.
 */
export function RightPanel(): JSX.Element {
  return <div>Right panel (Momentum) — not yet built</div>;
}
