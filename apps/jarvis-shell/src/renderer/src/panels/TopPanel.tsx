/**
 * TODO(Phase 1): port from the approved mockup — subscription usage bars,
 * taskers, spend total. See docs/design/README.md for the live artifact
 * link and ARCHITECTURE.md §2 for what's locked vs. still open on this
 * panel. Render from `@jarvis-ui/shared`'s `TopPanelState` once the WS
 * connection hook exists — don't invent a different shape.
 */
export function TopPanel(): JSX.Element {
  return <div>Top panel — not yet built</div>;
}
